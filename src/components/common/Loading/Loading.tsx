import { LoadingIcon } from '~images/svg';
import * as S from './Loading.styled';

const Loading = () => {
  return (
    <S.Container>
      <LoadingIcon />
    </S.Container>
  );
};

export default Loading;
